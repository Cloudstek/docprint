#!/usr/bin/env node
// @flow

import program from 'commander';
import docprint from '../src';

program
    .option('-p, --filepath <filepath>', 'Path to the blueprint file')
    .option('-d, --destination <destination>', 'Destination folder')
    .option('-h, --header <header>', 'Custom nunjucks/HTML header file or contents')
    .option('-c, --css <css>', 'Custom CSS file or contents')
    .option('-t, --template <file>', 'Custom nunjucks template file or one of the built-in templates')
    .parse(process.argv);

if (!program.filepath || !program.destination || program.destination.length === 0 || program.filepath.length === 0) {
    program.help();
    process.exit();
}

docprint({
    filepath: program.filepath,
    destination: program.destination,
    header: program.header,
    css: program.css,
    template: program.template
});
